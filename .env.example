# Cron expression for the poller. Default: every 30 minutes.
# getdeploying.com is hit ~22 times per snapshot — keep it polite.
POLL_CRON=*/5 * * * *

# Server port (also serves the built SPA in prod).
PORT=3001
